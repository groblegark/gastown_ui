# Gas Town UI Environment Variables
# Copy this file to .env and customize for your environment

# ============================================================================
# VITE CLIENT-SIDE VARIABLES (prefixed with VITE_)
# ============================================================================

# WebSocket URL for real-time updates
# Development: ws://localhost:8080/ws
# Production: wss://api.gastown.app/ws
VITE_WS_URL=ws://localhost:8080/ws

# Enable debug logging in production (optional)
# VITE_DEBUG=true

# Enable mock API for development/testing (optional)
# VITE_MOCK_API=true

# ============================================================================
# GASTOWN SERVER-SIDE CONFIGURATION
# ============================================================================

# --- Core Paths (Required) ---

# Root directory of the Gas Town installation
# This should point to the main gastown directory containing rigs, polecats, etc.
GASTOWN_TOWN_ROOT=/path/to/gastown

# Beads working directory - directory containing .beads folder
# Used for accessing beads CLI operations
GASTOWN_BD_CWD=/path/to/beads/cwd

# Gas Town home directory (optional, default: ~/.gastown)
# Contains configuration files and local state
# GASTOWN_HOME=~/.gastown

# --- CLI Binaries (Optional) ---

# Path or name of the gt (Gas Town) binary (default: 'gt')
# Use if gt is not in PATH or you want to use a specific version
# GASTOWN_GT_BIN=gt

# Path or name of the bd (Beads) binary (default: 'bd')
# Use if bd is not in PATH or you want to use a specific version
# GASTOWN_BD_BIN=bd

# --- Timeouts (Optional, in milliseconds) ---

# Default CLI command timeout (default: 30000 = 30 seconds)
# GASTOWN_CLI_TIMEOUT=30000

# Clone operation timeout (default: 180000 = 3 minutes)
# Clone operations are larger and need more time
# GASTOWN_CLONE_TIMEOUT=180000

# --- Polling Intervals (Optional, in milliseconds) ---

# Critical data polling interval (default: 5000 = 5 seconds)
# Used for frequently changing data: status, merge queue
# GASTOWN_POLL_CRITICAL=5000

# Standard data polling interval (default: 15000 = 15 seconds)
# Used for moderate-change data: convoys, work items
# GASTOWN_POLL_STANDARD=15000

# Background data polling interval (default: 60000 = 1 minute)
# Used for slow-changing data: rigs, health checks
# GASTOWN_POLL_BACKGROUND=60000

# --- Cache Settings (Optional) ---

# Cache time-to-live in milliseconds (default: 2000 = 2 seconds)
# GASTOWN_CACHE_TTL=2000

# --- Feature Flags (Optional) ---

# Enable write operations (default: false)
# When false, all mutating operations are blocked
# Set to true only in environments where writes should be allowed
# GASTOWN_ENABLE_WRITES=false

# Demo mode (default: true)
# When true, the UI shows demo data and blocks real operations
# Set to false for production use
# GASTOWN_DEMO_MODE=true
